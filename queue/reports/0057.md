# P2-0057 — Draft README.md for public release

## What was done

Wrote `README.md` at the repo root, all seven sections from the item file: pitch/status,
how it's built, sources & acknowledgments (three tiers), the self-improving system (honest
about what's not built), license intent, origin story, disclaimer.

## Verification performed

- `python quiz/build/rebuild.py` — 1176 questions, integrity ok.
- `python quiz/build/strict.py` — **96.3% (104/108)** on strictly-matched pairs, currently.
- `python quiz/build/check_notes.py` — 0 errors, 1 pre-existing warning (unrelated to this item).
- `docs/` confirmed untracked (`git ls-files docs/` → empty; `.gitignore` already excludes it),
  so the "none of these files are redistributed" claim in the README is currently true.

## Correction made to the item's own research

The item file asserted `Radiokomunikacije.pdf` "names no individual author, only 'izdaje
Hrvatski radioamaterski savez'." **I rendered printed page 6 (pdf index 3) of
`Radiokomunikacije_ocr.pdf` as an image and read it** (per BRIEF rule 7 — this is exactly
the "book itself does X" claim shape) rather than trusting `get_text()`. The page's own
"Uvod" **does** name individual authors: **Mladen Zadro, dipl. ing.** (voditelj/lead author),
co-authors **Ante Botica, dipl. ing.**, **dr. sc. Draško Marin**, and **Vladimir Štancl,
ing.**, reviewed by **dr. sc. Zvonimir Jakobović** and **mr. sc. Željko Ulip**, signed by HRS
president **mr. sc. Petar Miličić, 9A6A**. Screenshot saved at
`C:\Users\Tata\AppData\Local\Temp\claude\...\scratchpad\rk_p3.png` during this session (not
committed). The README credits the actual named authors instead of the item's "no author"
claim. Everything else the item stated about `ZBIRKA-1...` and `Pasaric_...` I independently
re-verified by rendering/reading their own title pages via `get_text()` (short, clean prose,
low OCR-garble risk) and it matched the item exactly — no other corrections needed there.

## Link verification (BRIEF rule 2)

Every tier-1 link was fetched this session, not just HTTP-200-checked:

- `narodne-novine.nn.hr/.../2022_12_150_2313.html` — fetched, title and NN 150/2022 /
  21.12.2022 confirmed.
- `docdb.cept.org` (CEPT's own document database) — **could not be verified**: WebFetch
  returned "unable to verify the first certificate" (broken/unverifiable TLS) on all three
  URLs tried (T/R 61-01, T/R 61-02, ERC Report 32 candidate links). Did not cite it.
- Instead linked CEPT T/R 61-01, T/R 61-02, ERC Report 32, and all of RA_ispiti_1/2/3/4 and
  HAKOM_Dopunsko_rjesenje_P_razred to their HRS's own `hamradio.hr/download/<same filename>`
  URLs, found via HRS's own "kako postati radioamater" page. Each was fetched and its
  reported byte size compared against the local `docs/` file — every one matched (e.g.
  RA_ispiti_2 local 2 948 798 B vs. fetched ≈2.8 MB; CEPT_Preporuka_TR61_02 local 367 457 B vs.
  fetched `/L 367457` exactly; ERCRep32 fetched metadata title "ERC Report 32" / subject
  "amended version" — exact match to local PDF metadata). This confirms same-document
  identity, not just a live URL. Documented in the README why `docdb.cept.org` was skipped
  (TLS) so a future reader doesn't wonder.
- HAKOM's own `hakom.hr/UserDocsImages/2021/Radiokomunikacije/Obvezni_dio_ispitnog_programa.pdf`
  was also checked and its metadata author ("Julijana Januia Bauer" — OCR-mangled diacritic)
  matches local `Obvezni_dio_ispitnog_programa.pdf` metadata ("Julijana Jančula Bauer",
  same creation date 14 Jan 2013) — confirmed same document, but the README links the
  RA_ispiti_4 copy on hamradio.hr instead (also confirmed) for consistency with the other
  eight tier-1 documents living on one page.

## Facts re-derived rather than copied

QUIZ_PLAN.md's cited book/HRS agreement figure (95.3%, 102/107) is now stale — a live
`strict.py` run this session shows **96.3% (104/108)**. Used the current, freshly-verified
number in the README rather than the item's suggested 95.3%, since BRIEF's mandatory verify
step surfaced the current figure directly and citing a number my own verification run just
contradicted would be worse than the small deviation from the item's literal text.

## Not done / left alone

- No `LICENSE` file added (explicitly out of scope per item).
- No GitHub Pages link (P2-0058, blocked on Glaukon's setup — placeholder text used verbatim
  as the item suggested).
- Did not touch `PLAN.md` or `QUIZ_PLAN.md` content, only linked them.
- Did not touch `docs/`, `.gitignore`, or any file outside `README.md`.

## Files touched

- `README.md` (new) — staged with `git add`.
