# P2-0051 — fix bk-pra-2-04 fabricated callsign quote, land skupina II pravila notes (Round 9)

## What this round did

Recovered the stash `queue P2-0051: fail` (the eighth landing attempt's work), which already
had the item's original two fixes in place (`bk-pra-2-04` "0A9Z" -> "9A9Z"; `bk-pra-2-39`
"12KOF3E" -> "12K0F3E" — both re-verified this round by direct render, see below).

Round 9 named two further known defects to fix, both single-word quote-verbatim slips found
by the eighth-round checker:

1. `bk-pra-2-05` quoted UFB as "izvanredno"; rendered str. 255 prints "zvanredno" (no leading
   "i"). Fixed by removing the "i".
2. `bk-pra-2-37` quoted "gotovo uvijek rabe antene..."; rendered str. 173 reads "gotovo **se**
   uvijek rabe antene...". Fixed by inserting "se".

Both were rendered at 250dpi and read directly off the page image before editing — not off
`booktool.py` extraction — per rule 7.

## Full re-render pass (per Round 9's explicit instruction)

Round 9 said not to assume the rest of the file is clean and to render every page this file
cites, checking every quoted string character-by-character against the image. Given this
file's history (eight straight rounds each finding one more slip), I did this rather than
stopping at the two named defects.

All 26 distinct printed pages cited anywhere in the file's 40 notes (40, 51, 144, 173, 210,
227, 251, 252, 253, 254, 255, 256, 257, 258, 259, 264, 265, 266, 267, 271, 273, 278, 279, 280,
383, 386) were rendered via PyMuPDF at 250dpi (some re-rendered at 500-600dpi for close
character-level reading) and read directly — none of this verification relied on
`booktool.py`'s extracted text as evidence of what the book shows, per rule 7. I first
narrowed to the notes that make an explicit "the book states X" claim (grep for `doslovno`,
`izravno navodi`, `piše da`, `ispisuje`, `definira upravo tako`, `knjiga to/ga/je/izravno`,
etc. — 12 notes matched), verified those quotes word-for-word against renders, and separately
spot-checked every table-cited value (callsign country lists, band-plan segments, repeater
frequency tables) against renders since those are also copied literal data even without
"doslovno" framing.

This found **two further genuine defects** beyond the two Round 9 named:

3. `bk-pra-2-35` — the note says "knjiga izravno navodi da se 'radioamaterska pozivna
   oznaka sastoji se od predmetka (prefiksa)...'" (correct standard spelling
   "radioamaterska"). Rendered str. 256 at 500dpi shows the book itself prints
   "**Radiomaterska** pozivna oznaka..." — missing the second "a" (a genuine book
   typesetting error, confirmed by both the PyMuPDF text extraction and a 500dpi crop
   render, so not an extraction artifact). Fixed by changing the note's quote and the
   `cite.loc` quote to "radiomaterska"/"Radiomaterska" to match what the book actually
   prints, and added a short parenthetical in the note body flagging it as the book's own
   typo (mirrors how `bk-pra-2-26`'s duplicate "c)" label was already treated elsewhere in
   this file — noted, not silently corrected).
4. `bk-pra-2-24` — the note quotes QSB as "'promjena jakosti signala — fading'" (em dash).
   Rendered str. 251 at 600dpi shows the book prints "promjena jakosti signala **(fading)**"
   — parentheses, not a dash. Fixed by changing the quote to use parentheses.

## Pages/notes checked and confirmed correct (no change needed)

Explicit-quote notes confirmed exact-match against renders: `bk-pra-2-03` (CQ, str.
227/252), `bk-pra-2-04` (str. 383, re-confirmed), `bk-pra-2-05` (str. 255/383, fixed as
above), `bk-pra-2-07` (str. 227/251), `bk-pra-2-09` (str. 251), `bk-pra-2-10` (str. 253/254),
`bk-pra-2-11` (str. 266), `bk-pra-2-22` (str. 227/254), `bk-pra-2-25` (str. 144), `bk-pra-2-31`
(str. 386), `bk-pra-2-37` (str. 173, fixed as above), `bk-pra-2-40` (str. 210).

Table/list citations spot-checked against renders and confirmed exact: `bk-pra-2-01/29/38`
(band plan segments, str. 264-266), `bk-pra-2-12` (microwave allocation notes, str.
278/279), `bk-pra-2-13` (str. 273), `bk-pra-2-14/28` (repeater channel table, str. 280),
`bk-pra-2-15/16/17/32/33/34` (DXCC callsign-prefix lists, str. 256-259), `bk-pra-2-18`
(str. 253/264/267), `bk-pra-2-39` (str. 271, re-confirmed).

Not re-opened per the item's original "Avoid" instruction not to re-litigate notes already
found clean in prior rounds, beyond what the render pass above covered: the remaining ~10
notes without an explicit verbatim-quote claim and without a table citation (mostly single
abbreviation glosses already cross-checked incidentally while reading pages 251-255 for other
notes).

## Verification

- `python quiz/build/rebuild.py`: 1176 questions, `integrity ok: 1176 questions, all
  answerable`, no unknown-id warnings. All 40 `bk-pra-2-*` ids present in
  `notes_book_s2_pravila.json`.
- `python quiz/build/strict.py`: 104/108 = 96.3% — matches the required floor exactly, no
  regression. The 4 disagreements listed are all in other files (`bk-teh-*`, `bk-pro-*`),
  unrelated to this item.
- `python quiz/build/check_notes.py`: 0 errors, 1 pre-existing warning in an unrelated file
  (`teh3_krugovi_a/teh-103: no citation`), not touched by this item.
- `link_check.py` not run — no links added or changed.

## File status

`quiz/build/notes_book_s2_pravila.json` was never committed (first-time landing across 9
attempts). Staged with `git add` this round, not committed (orchestrator commits).

## Recommendation

This is the ninth landing attempt and the fourth consecutive round to find "one more" defect
after a full re-render (rounds 6, 7, 8, 9 each found 1-2). This round went further than prior
rounds by rendering every cited page and checking every quote-bearing note, not just the
named defects, specifically to try to break this cycle. If the checker finds a further defect
this round, it is worth strongly considering Glaukon's suggestion from the P2-0051.why blocked
note: accept a slightly looser quote-fidelity bar for this file rather than continuing
round-by-round patching, since the defect size has been shrinking to single letters/punctuation
marks with diminishing signal-to-noise on each pass.
