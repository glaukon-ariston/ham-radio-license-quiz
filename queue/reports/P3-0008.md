# P3-0008 — Book notes, skupina I pravila i postupci

## What this run did

This item had been attempted before and blocked on one citation defect. Per the item's
"Recovering the blocked attempt" section I recovered the stashed work rather than rewriting:

```
git stash list                          -> stash@{5}: On queue-drain: queue P3-0008: fail
git stash show --stat stash@{5}         -> (empty; the stash holds UNTRACKED files only)
git stash show --stat --include-untracked stash@{5}
                                        -> queue/reports/P3-0008.md, quiz/build/notes_book_s1_pravila.json
git checkout stash@{5}^3 -- <those two paths>
```

Trap worth recording: `git stash show --stat stash@{5}` prints **nothing** for this stash,
because both files were new/untracked and live in the stash's third parent (`^3`), not in the
usual `stash@{N}` diff. A future recoverer who runs only the plain `--stat` will conclude the
stash is empty and rewrite 40 notes from scratch. Use `--include-untracked`, and read the files
with `git show 'stash@{N}^3:<path>'`.

The stash was left in place (not popped/dropped) — nothing here needs it any more, but dropping
stashes is not this item's business.

## Defect 1 (the blocker) — `bk-pra-1-27`, repaired

Verified the item's diagnosis first-hand against the Pravilnik PDF with PyMuPDF. NN150/22
čl. 2. st. 1. reads, word for word:

> 1. amaterska radijska postaja: radijska postaja koja radi u radiofrekvencijskom pojasu
> namijenjenom amaterskoj službi i amaterskoj satelitskoj službi,

That is a *band-based* definition. It does not contain the equipment-based wording
("jedan ili više odašiljača i prijamnika … s pomoćnom opremom") that the note was using it to
support. The item's verdict is correct.

Repair: the `NN150/22 čl. 2. st. 1. t. 1.` entry is **removed from the `cite` array**. The note
now cites only the RK exam text, which I confirmed word-for-word on printed page 381 (pdf index
374), question 27, option c: *"c) jedan ili više odašiljača i prijamnika s pomoćnom opremom,"*.
The note's prose now says explicitly that this definition exists only in the priručnik's own exam
text and that NN150/22 čl. 2. st. 1. t. 1. defines the station by band, not equipment, and is
therefore deliberately **not** cited as its source. (The article number survives in prose only as
a stated *contrast*, never as support; it is verified, so this is not an invented pointer.)

## Defect 2 (found during re-verification) — `bk-pra-1-29`, repaired

The note cited `NN150/22, Dodatak 1., Tablica 1. (redak 14000 – 14350 kHz)`. **No such row
exists.** DODATAK 1. / 1. TABLICA 1. splits 20 m across two rows in the A-razred block:

```
14 000 – 14 250 kHz   1     Pex   1500
14 250 – 14 350 kHz         Pex   1500
```

This is exactly the class of defect the item was blocked for — a real document, a plausible-
looking row label, and no such text on the page. Corrected to
`Dodatak 1., Tablica 1. (retci 14 000 – 14 250 kHz i 14 250 – 14 350 kHz, A razred)`.

(The neighbouring rows the other two Dodatak-1 citations name *do* exist verbatim:
`3500 – 3800 kHz` for `bk-pra-1-15` and `430 – 440 MHz` for `bk-pra-1-28`. Also confirmed the
table has a single `1. TABLICA 1.` heading covering both an A-razred and a P-razred block, so
"Tablica 1." is the right label for all three.)

## Defect 3 — `bk-pra-1-06`, note prose contradicted its own cited page

The note glossed `GD = dobar`. Resolving the OCR column scramble on printed 253 by word
coordinates (`page.get_text("words")` grouped by y, not by reading order — the plain text dump
interleaves the label and meaning columns and is not safe to align by eye) gives:

```
77  - dobar dan
78  GD
104 GUD - dobar
```

So the priručnik assigns `GD = dobar dan` and `GUD = dobar`. Reworded: GD here stands for English
"good" as a signal report, with the table's own `GD = dobar dan` / `GUD = dobar` split called out
so the student is not misled when they look the abbreviation up. No answer touched.

## Defect 4 — `bk-pra-1-33`, primary claim was uncited

The note's operative claim is "respond to the distress call and try to notify the authorities".
Its only cite was RK printed 256, whose section `4. MEĐUNARODNI ZNAKOVI ZA NESREĆE …` contains
just the two distress signals ("SOS", "MAYDAY") — not the duty. NN150/22 čl. 19. st. 3. states
it directly:

> (3) Prigodom prijama poziva u pomoć u slučaju nesreće i opasnosti radioamater mora bez odgode
> prekinuti vlastito odašiljanje te uspostaviti komunikaciju s radijskom postajom koja odašilje
> poziv u pomoć.

Added as a second cite and paraphrased into the note. This is a citation repair inside the item's
own file, in the same class as the blocker; flagging it here since it goes marginally beyond
"re-confirm".

## Full citation re-verification (the item's added Done-when)

Every citation in the file was re-read on its own cited page this run, with `booktool.py` /
PyMuPDF, `PYTHONIOENCODING=utf-8`. Nothing was accepted from the previous attempt's report.

RK — printed pages, each resolved through `booktool.py`'s folio map and dumped:

| cited | confirmed on the page |
|---|---|
| str. 250 `2. KRATICE Q-KODA` (uvodni tekst) | heading present; "*rabe se kratice za mnoge pojmove i postupke čime se znatno skraćuje vrijeme potrebno za prijenos poruke*" — supports `bk-pra-1-40` verbatim |
| str. 251 Q-kod | QRB "udaljenost između radiopostaja", QRL "zauzetost, rad; molim ne ometati — radim!", QRM "smetnje", QRN "smetnje od elektrostatičkog pražnjenja", QRP "smanjenje snage odašiljača", QSL "potvrda prijama", QTH "smještaj radiopostaje", QTR "točno vrijeme", QRA "ime postaje" — all present |
| str. 252 | AGN, CALL, CONTEST, COQ "Opći poziv svim radiopostajama", CRD "QSL karta" (OCR "QOSL"), CW "telegrafija", DR "drag" |
| str. 253 | DX "jako udaljen, daleko, daleka veza", ES, FER "za", GE "dobra večer", GD, GUD, IARU |
| str. 254 | MNI, OK, OM, PSE "molim", R "primljeno, u redu", SIGS (OCR "siIGS"), SKED "dogovorena radijska veza" |
| str. 255 | SWL "prijamni radioamater", TNX/TU "hvala", UR "vaš, tvoj", VY "vrlo" |
| str. 256 | `4. MEĐUNARODNI ZNAKOVI ZA NESREĆE …` with SOS/MAYDAY; `5. POZIVNE OZNAKE` with "*pozivna oznaka sastoji se od predmetka (prefiksa) … i sufiksa koji određuje radiopostaju unutar države*" — `bk-pra-1-01` verbatim |
| str. 256-262 prefiksi | see below |
| str. 264 | `6. PLANIRANJE POJASEVA IARU-a`; "3 590…3600 kHz digitalna (preporučljivo paketna), paketna AX25, telegrafija" and "3600…3620 kHz digitalna, telefonija, telegrafija" — `bk-pra-1-02` exact |
| str. 265 | "14 060 … 14 070kHz telegrafija" — `bk-pra-1-31` exact |
| str. 266 (Napomene) | "*Na frekvencijskim pojasevima do 10 MHz upotrebljava se LSB (donji bočni pojas), a iznad 10 MHz upotrebljava se USB (gornji bočni pojas).*" — `bk-pra-1-34` verbatim |
| str. 381 | SKUPINA I questions 25-32 with options verbatim, incl. q27 option c and q32's "PSE UR OTH AGN +K" |

**Prefix citations are stronger than the previous attempt believed.** That report said the DXCC
table's OCR was too column-scrambled to pin TA and OZ and fell back on structural corroboration
plus general knowledge. It is not: read by word coordinates the by-prefix list on printed 258 is
clean, and every one of the six is a literal row —

```
sv Greece      TA Turkey      Oz Denmark      PA Netherlands      sP Poland
```

(the lowercase forms are OCR of SV / OZ / SP), cross-checked against the by-country list on
printed 260-262 (`Greece sv`, `Denmark Oz`, `Netherlands PA`, `Poland sP`). For `G3FOG`, printed
257 lost the bare "G" label to OCR but the row reads "… England" immediately above the
GD/GI/GJ/GM/GU/GW block (`Isle of Man`, `Northern Ireland`, `Jersey`, `Scotland`, `Guernsey`,
`Wales`), which also confirms `bk-pra-1-37`'s parenthetical GM/GW/GI gloss. All six sit inside the
cited str. 256-262 span; the citations stand as written and now rest on the table itself.

NN150/22 (no printed folios — cited by članak, per the file's convention), read directly from the
PDF text layer:

- čl. 2. st. 1. t. 2. — "*radioamater: fizička osoba koja se bavi radijskom tehnikom i
  radiooperatorstvom isključivo iz osobnih razloga i bez novčane naknade*" — `bk-pra-1-13` verbatim.
- čl. 15. st. 1. — "*Pozivna oznaka sastoji se od oznake »9A«, jedne znamenke i najviše tri
  slova.*" — `bk-pra-1-01`'s "do tri slova" verbatim.
- čl. 19. st. 3. — quoted above, new cite on `bk-pra-1-33`.
- Dodatak 1. Tablica 1. — rows `3500 – 3800 kHz`, `430 – 440 MHz` confirmed; `14000 – 14350 kHz`
  does not exist (defect 2).
- čl. 2. st. 1. t. 1. — confirmed to *not* support `bk-pra-1-27` (defect 1).

## Left alone, named per the brief's "do not widen scope"

Three notes describe operating *practice* while their cite anchors only the *abbreviation*:

- `bk-pra-1-07` and `bk-pra-1-22` (SWL neither transmits nor withholds QSL cards) cite
  "SWL, str. 255", which says only "SWL — prijamni radioamater".
- `bk-pra-1-39` (QSL cards go via the bureau and by post) cites "CRD — QSL karta, str. 252",
  which says only that CRD means QSL card.

I searched the whole RK book for "QSL biro" / "biro" / "QSL kart" and the Pravilnik for any QSL
or SWL sending provision: **there is no such text anywhere in either source.** Per rule 5 the
right move is to leave the abbreviation cite standing rather than attach a plausible-looking one,
so I did not touch them. Worth a future item if these three should carry an explicit
"no source in the corpus; standard practice" marker.

Also noted, no action: `bk-pra-1-04` glosses `INFO = informacija`, which is absent from the
abbreviation table — legitimately so, since printed 255 closes with "*Ovdje nisu napisane riječi
koje u potpunosti odgovaraju riječima engleskog jezika*". The cite (span 252-255) is fine.

No HRS/book answer was changed anywhere. No new flag was warranted in this packet; no existing
flag was dropped (this file carries none — the amber `prv-035`/`prv-057` and the four `strict.py`
disagreements are all outside these 40 ids).

## Verification

- `python quiz/build/rebuild.py` — **integrity ok: 1176 questions, all answerable**;
  `questions.json hrs=456 extra=720 explained=456 figures=52`; coverage
  `tehnicki 282/282, propisi 95/95, pravila 79/79`. The `! notes for unknown ids` line lists
  every `bk-*` note in the repo (all shipped `notes_book_*` files, ~700 ids), not just this
  file's 40 — pre-existing, already flagged from P3-0006, not a regression from this item.
- `python quiz/build/strict.py` — **96.3% (104/108)**, 4 disagreements: `teh-201`, `teh-273`,
  `pro-090`, `pro-047`. Same four as before; none in this packet. Notes files do not feed
  `strict.py`, so no regression is possible from this change and none occurred.
- `python quiz/build/check_notes.py` — checked 15 batches, 443 notes, **0 errors, 1 warning**
  (`teh3_krugovi_a/teh-103: no citation`, pre-existing and unrelated). It does not glob
  `notes_book_*`, so this file was validated by hand: JSON parses as UTF-8, exactly 40 non-`_`
  keys, `bk-pra-1-01`..`bk-pra-1-40`, an exact set match against the packet's 40 ids, and every
  note carries a non-empty `cite`.
- `python quiz/build/link_check.py` — **not run, correctly**: the file contains no links
  (`grep -c http` = 0). Nothing external was added, so there is no fetch-and-confirm obligation.

## Files touched

- `quiz/build/notes_book_s1_pravila.json` — in `files:`; recovered from the stash, then the four
  repairs above plus a `_note` header refresh naming the exact articles now cited and recording
  the `bk-pra-1-27` source caveat. Staged.
- `queue/reports/P3-0008.md` — this file. Staged.
- `quiz/build/work/packet_book_s1_pravila.json` — also in `files:`; read only, not edited.
- Nothing outside the item's `files:` list was modified. Build artifacts regenerated by
  `rebuild.py` (`quiz/questions.json`, `quiz/quiz.html`, `quiz/build/bank.json`,
  `quiz/build/book_parsed2.json`) are gitignored and unstaged.
