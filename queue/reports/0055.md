# P1-0055 — Untrack docs/ before public release

## What was done

1. **`.gitignore`**: added a `docs/` entry at the bottom of the file, matching the existing
   style (a `# --- ... ---` comment line above the pattern, same as the `emails/` PII block
   immediately preceding it):

   ```
   # --- copyrighted source scans: HRS priručnik + third-party book; credited, not redistributed ---
   docs/
   ```

2. **Untracked `docs/`**: ran `git rm -r --cached docs/`, which staged 21 deletions from the
   git index (all files under `docs/`, e.g. `Radiokomunikacije.pdf`,
   `Radiokomunikacije_ocr.pdf`, `Pasaric_Radioamaterizam_za_mlade.pdf`, the `RA_ispiti_*.pdf`
   exam-bank set, `Pravilnik_o_amaterskim_radijskim_komunikacijama_NN_150_22.pdf`, etc.).
   `git status --short` shows these as `D` (staged deletion from index), not touched on disk.

3. Staged `.gitignore` with `git add`.

## Verification

- `git ls-files docs/` — prints nothing (0 lines). Confirmed untracked.
- `ls docs/` (disk) — still shows all 21 files present, sizes unchanged. Nothing deleted.
- `python quiz/build/rebuild.py` — **1176 questions, integrity ok, no aborts.** `docs/`
  removal from the git index doesn't affect `rebuild.py`, which reads `docs/` by relative
  path from disk regardless of git tracking state — exactly as the item's "Avoid" section
  anticipated.
- `python quiz/build/strict.py` — **96.3% (104/108)**, same 4 pre-existing HRS-vs-book
  disagreements as the last recorded run (queue/reports/P3-0053.md: 96.3%, 104/108, "same 4
  pre-existing disagreements"). No regression.
- `python quiz/build/check_notes.py` — 0 errors, 1 pre-existing warning
  (`teh3_krugovi_a/teh-103: no citation`), unrelated to this change.
- `link_check.py` not run — no links were added or changed by this item.

## Scope notes

- Did not touch any `quiz/build/*.py` script.
- Did not delete anything from disk.
- Did not attempt a git-history rewrite — that's P1-0056, explicitly out of scope here. The
  21 files remain recoverable from prior commits; this change only stops future commits from
  re-tracking them.
- No other files were touched.
