bk-teh-5-54's second citation reads "3.4.6. Harmonici... tiskana str. 121", but the quoted examples ("Drugi harmonik je f2=2x3530 kHz=7060 kHz"; "Peti harmonik... f5=720 MHz") are actually on tiskana str. 120 under section 3.4.5 (p.121 only has the 3.4.6 heading, not these examples). All other 29 notes and 9 sampled citations verified correct; the report's "stray script re-ran harmlessly" claim also independently confirmed true (notes_book_s2_propisi.json untouched). Work stashed: 'queue P3-0023: fail'.

2026-08-11, repair attempt 2 (stash "queue P3-0023: fail (repair attempt 2)"): the named
bk-teh-5-54 defect was fixed correctly (examples are on str. 120 / 3.4.5), plus eleven more
bad citations the first checker passed — including bk-teh-5-58 citing section 10.2 when 10.2
is "Munja" on str. 208, and one false negative (bk-teh-5-26 claimed no source supports Es MUF
220 MHz; printed str. 170 says it verbatim — the earlier search missed it because the book
writes "E_s" and never spells "sporadič" on that page). The checker verified all 47 distinct
printed page references, not a sample, and all three absence claims by page render.

Blocked again on ONE defect: bk-teh-5-20 explains the wrong circuit element. Printed str. 335
asks "kondenzator C3 je:" (the packet OCR dropped the subscript to "C,"), and C3 in the q19
schematic is the emitter bypass across R4 — but the note says "Kondenzator C1 o kojem je riječ
je vezni/blokadni kondenzator na ulazu ili izlazu pojačala" and builds its whole explanation on
coupling-capacitor behaviour. The answer letter (c) is unaffected; the note simply points the
student at the wrong part of the figure. Recover the attempt-2 stash, not the original — it is
12 fixes ahead.

Cosmetic, same file: the _note says "Ispravljeno je devet citata" then lists twelve.

2026-08-11, repair attempt 3 (stash "queue P3-0023: fail (repair attempt 3)"): fixed the
named bk-teh-5-20 C1-vs-C3 defect (confirmed correct: q19/q20 genuinely asks about C3, the
emitter-bypass capacitor across R4) but the replacement citation to str.83 claims that page's
circuit has "isti nazivi C1-C4/R1-R4" (the same C1-C4/R1-R4 component names) as the q19
schematic. Str.83's circuit (Sl. 2.6.5) only has C1-C3, no C4 - confirmed by rendered page
image. The claim contradicts the note's own adjacent quote ("Sva tri kondenzatora, C1, C2,
C3..."). The underlying physics explanation is otherwise sound and well-supported by str.83's
text; only the "same 4-capacitor topology" framing is wrong. 7 of 9 other figure-bearing notes
spot-checked clean for component/curve/quantity accuracy, not just citation presence.

This is the file's fourth confirmed defect across three repair rounds. NOT refiling again
automatically - flagging for human review, matching the treatment given to sibling items
P3-0012 and P3-0016, which hit the same "keeps finding one more thing" pattern.

Resolved 11 Aug 2026 by human review (Glaukon independently rendered tiskana str. 83 and
confirmed the circuit has three capacitors, not four). Recovered stash "queue P3-0023: fail
(repair attempt 3)" and applied a one-word fix: "isti nazivi C1-C4/R1-R4" -> "isti nazivi
C1-C3/R1-R4" in both bk-teh-5-20's citation and its note prose. No other change. rebuild.py:
1176 questions, explained 1066->1096, no unknown-id warnings. strict.py: 96.3% (unchanged).
check_notes.py: 0 errors, 1 pre-existing unrelated warning. Item moved to queue/done/.
